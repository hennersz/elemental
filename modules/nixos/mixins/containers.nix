{
  virtualisation.docker = {
    enable = true;
    autoPrune = {
      enable = true;
    };
  };
  virtualisation.podman = {
    enable = true;
    autoPrune = {
      enable = true;
    };
  };
}
